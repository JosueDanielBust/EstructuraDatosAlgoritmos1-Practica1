#!/usr/bin/env ruby

require 'rubygems'
require 'main'
require 'board'

$stdout.print "> Give me the n: "
$stdout.flush

n = gets.to_i

puts "n: #{n}"

$game = Game.new(n)

begin
  $game.initGame()
rescue
  puts "Unknow exception"
end
