#!/usr/bin/env ruby

require 'rubygems'
require 'main'
require 'board'
require 'matrix'

$stdout.print "> Give me the n: "
$stdout.flush
n = gets.to_i

$game = Game.new(n)

begin
  $game.initGame()
rescue
  puts "Unknow exception"
end
